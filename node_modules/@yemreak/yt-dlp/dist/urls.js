export function isInstagramURL(url) {
    return url.toLowerCase().includes("instagram.com/");
}
export function isYoutubeURL(url) {
    const lowerUrl = url.toLowerCase();
    return lowerUrl.includes("youtube.com/") || lowerUrl.includes("youtu.be/");
}
export function isTwitterURL(url) {
    return url.toLowerCase().includes("x.com/");
}
export function isTedURL(url) {
    return url.toLowerCase().includes("ted.com/talks/");
}
export function parseLangFromTedUrl(url) {
    return url.match(/(?<=language=)(\w+)/)?.[0];
}
//# sourceMappingURL=urls.js.map